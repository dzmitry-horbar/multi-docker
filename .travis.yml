sudo: required
services:
    - docker

before_install:
    - docker build -t dmitry-gorbar/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true dmitry-gorbar/react-test npm test -- --coverage

after_success:
    - docker build -t dmitry-gorbar/multi-client ./client
    - docker build -t dmitry-gorbar/multi-nginx ./nginx
    - docker build -t dmitry-gorbar/multi-server ./server
    - docker build -t dmitry-gorbar/multi-worker ./worker
    # log in to docker cli
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # take those images and push them to docker hub
    - docker push dmitry-gorbar/multi-client
    - docker push dmitry-gorbar/multi-nginx
    - docker push dmitry-gorbar/multi-server
    - docker push dmitry-gorbar/multi-worker
